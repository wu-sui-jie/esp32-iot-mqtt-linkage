# ESP32 八模块联动改造 · 任务规划

> 项目：物联网项目实践 第 1 组大作业
> 协议依据：《MQTT 通信协议规范_第1组》V2.0
> 文档日期：2026-09-19
> 当前状态：**阶段 0（基线备份）已完成**，阶段 1 待启动

---

## 一、现状梳理

### 1.1 八个模块与代码形态

板号、设备名严格按协议表 7 与表 A-1：

| 板号 | 模块 | device | 类型 | 工程目录 | 代码形态 | MQTT 库 |
|---|---|---|---|---|---|---|
| 1 | 舵机 | servo | 执行器 | `舵机/` | 多文件（config+proto+servo+main） | 256dpi/MQTT |
| 2 | 继电器（风扇） | fan | 执行器 | `风扇板_2号/` | 单文件 main.cpp | PubSubClient |
| 3 | 红外对射 | ir_beam | 传感器 | `红外/esp32_ir_beam/` | 单文件 .ino | PubSubClient |
| 4 | 温湿度 SHT30 | sht30 | 传感器 | `sht30_board4/` | 多文件（config+proto+sht30+main） | 256dpi/MQTT |
| 5 | ADC/RGB/蜂鸣器 | adc、rgb、buzzer | 综合 | `rgb_board5/` | 多文件（config+proto+rgb+main） | 256dpi/MQTT |
| 6 | 火焰 | flame | 传感器 | `火焰板_6号/` | 单文件 main.cpp | PubSubClient |
| 7 | 智能语音播报 | tts | 执行器 | `智能语音/` | 多文件（config+proto+tts+linkage+main） | 256dpi/MQTT |
| 8 | 光照度 | light_sensor | 传感器 | `光照/esp32_light_ir/` | 单文件 .ino | PubSubClient |

每个目录都是**独立的 PlatformIO 工程**，可单独编译、单独烧录。这个结构在改造中必须保留。

### 1.2 代码存在两套技术栈（这是所有问题的根源）

**A 套（吴遂杰、吴一凡：1、4、5、7 号板）**

- 分层：`my_config.h`（配置）+ `mqtt_proto.h/.cpp`（协议层）+ 模块文件 + `main.cpp`
- 用 `256dpi/MQTT`，支持 QoS 1，符合协议表 18
- 回调只解析入队，发送在主循环做（避开 arduino-mqtt 的 publish 死锁）
- 已内置两个弱函数钩子：`device_busy()`（回执挂起）、`proto_on_other()`（旁听其他板报文）
- 有 `SUB_EXTRA_REPORT` / `SUB_EXTRA_ONLINE` 宏控制订阅范围

**B 套（李豪、施昱名：2、3、6、8 号板）**

- 单文件，配置是文件顶部的 `const` 变量
- 用 `PubSubClient`，**只能发 QoS 0**，做不到协议表 18 要求的「evt、ack 用 QoS 1」
- 回调里直接执行动作并 publish
- 没有协议层抽象，没有钩子，没有订阅扩展能力

### 1.3 已探明的四个阻塞问题

**问题 1：MQTT 登录凭据不一致**

- 2 号板、6 号板写了 `MQTT_USER = "shiyuming"`（`风扇板_2号/src/main.cpp:25`、`火焰板_6号/src/main.cpp:31`）
- 其余六块板是匿名连接
- 协议表 21 明确要求「用户名/密码：留空」
- 现状能跑，说明服务器允许匿名；但那两块板的用户名是多余的，且一旦服务器启用认证就会行为分叉

**问题 2：变量命名完全对不上**

同一个概念在两套代码里至少有 2～3 个名字：

| 概念 | A 套叫法 | B 套叫法 |
|---|---|---|
| WiFi 名 | `WIFI_SSID` | `SSID` / `WIFI_SSID` / `WIFI_PASS` |
| 服务器 | `mqttServer` | `MQTT_SERVER` / `MQTT_HOST` |
| 板号 | `ID` | `BOARD_ID` / `ID` |
| 板号字符串 | `String(ID)` 现算 | `SRC` / `BOARD_ID_STR` |
| MQTT 客户端对象 | `client`（`net` 是 WiFiClient） | `client`（`espClient`/`wifiClient` 是 WiFiClient） |
| 序号 | `tx_seq` + `next_seq()` | `seq` + `nextSeq()` |

注意 B 套的 `SSID`、`PASSWORD` 这两个名字**碰不得**——ESP32 的 `WiFi.h` 里有同名方法，宏展开会直接编译失败（A 套的 readme 已记录此坑）。

**问题 3：5 号板蜂鸣器完全没有实现**

`rgb_board5/src/my_config.h:39` 写着 `#define HAS_BUZZER 0`，代码里一行蜂鸣器逻辑都没有。协议表 12 给蜂鸣器定义了必须实现的 `set_power`，表 20 还给了选做的 `beep`。

**问题 4：智能语音模块的播报能力与数据产生速度不匹配**

这是任务 5 要解决的核心问题，量化分析见 6.1 节。

---

## 二、任务分解与执行顺序

改造分六个阶段。**顺序不可调换**，理由写在每个阶段的「为什么在这个位置」里。

```
阶段 0  基线备份（GitHub）              ← 本轮已完成
   ↓
阶段 1  命名统一 + 工程结构归一（任务2）  ← 所有后续工作的地基
   ↓
阶段 2  蜂鸣器实现（任务3）              ← 独立，可单独验证硬件
   ↓
阶段 3  协议层扩展（联动上报通道）        ← 阶段 4、5 的公共依赖
   ↓
阶段 4  四条联动规则（任务1）            ← 阶段 5 需要它上报的事件
   ↓
阶段 5  智能语音重新设计（任务5）
   ↓
阶段 6  全量编译验证 + 八板联调
```

---

### 阶段 0 · 基线备份（已完成）

| 项目 | 内容 |
|---|---|
| 目标 | 冻结当前可工作的状态，之后任何改动都能回滚对比 |
| 产出 | GitHub 公开仓库 + 首次提交 |
| 验收 | 克隆下来能看到 8 个模块的源码与报告 |

已处理：`.pio/`（293 MB 构建产物）、`.vscode/`、`.deep-copilot/` 全部排除，只提交源码与文档。

---

### 阶段 1 · 命名统一与工程结构归一（任务 2）

| 项目 | 内容 |
|---|---|
| 目标 | 8 个工程用同一套命名、同一套分层、同一个 MQTT 库；功能行为不变 |
| 改动 | 2、3、6、8 号板从 B 套迁移到 A 套；4 个 A 套工程的命名按第三章规范对齐 |
| 产出 | 8 个工程全部为 `my_config.h + mqtt_proto + 模块 + main.cpp` |
| 验收 | ① `pio run` 八个工程全部编译通过；② 串口输出与改造前逐条对照一致；③ MQTTX 发协议附录 B 的命令，行为与改造前相同 |
| 负责人 | 各模块原作者负责自己的板（见第七章） |
| 为什么在这个位置 | 后续三个任务的代码都写在统一命名之上；若先做功能再统一，等于重写两遍 |

**换库是硬要求**：协议表 18 规定 evt、ack 用 QoS 1，而 `PubSubClient.publish()` 只能发 QoS 0。A 套的 readme 已经踩过这个坑并写明「必须换库，不换库编译都过不了」。所以 2、3、6、8 号板必须换成 `256dpi/MQTT`。

**文件结构保持不变**：只改目录内部的文件组成，模块文件夹本身、以及 `光照/esp32_light_ir/`、`红外/esp32_ir_beam/` 这两个子目录路径都保留（`platformio.ini` 里的 `src_dir` 继续指向它们）。`.ino` 文件改名为 `main.cpp` 并显式声明函数原型——因为 PlatformIO 对 `.ino` 的自动原型生成与 C++ 模块文件混用时会出问题，而其余六个工程都用 `main.cpp`。

---

### 阶段 2 · 蜂鸣器实现（任务 3）

| 项目 | 内容 |
|---|---|
| 目标 | 5 号板补上蜂鸣器，实现协议表 12 的 `set_power` 与表 20 的 `beep` |
| 改动 | `rgb_board5/`：新增 `buzzer.h/.cpp`，`HAS_BUZZER` 改为 1 |
| 产出 | 蜂鸣器可被平台控制，也可被火焰联动触发 |
| 验收 | MQTTX 发 `{"device":"buzzer","action":"set_power","param":{"on":true}}` → 蜂鸣器持续响、ack 回来；发 `on:false` → 停；发 `beep` + `duration:500` → 响 0.5 秒自动停 |
| 负责人 | 吴一凡（5 号板原作者） |
| 为什么在这个位置 | 与联动解耦，先做完就能单独验证蜂鸣器接线是否正确；阶段 4 的火焰报警直接复用它 |

**必须用非阻塞方式**：`beep` 的 `duration` 最长 5000 ms，绝不能 `delay(5000)`。主循环里跑着 `proto_loop()`，卡住 5 秒会被 MQTT 服务器判超时踢下线。做法与舵机模块同款——记下"该停的时刻"，在 `buzzer_loop()` 里每轮检查一次。

---

### 阶段 3 · 协议层扩展（联动上报通道）

| 项目 | 内容 |
|---|---|
| 目标 | 让联动板能"旁听"其他板的上报，并把自己的联动动作广播出去 |
| 改动 | ① `mqtt_proto.cpp` 在四个工程里同步新增 `proto_send_evt()`（发事件）；② 给需要联动的板打开 `SUB_EXTRA_REPORT`；③ 在 `proto_on_other()` 里加 `evt` 分支 |
| 产出 | 统一的"订阅 → 判断 → 执行 → 上报"通道 |
| 验收 | 用 MQTTX 向 report 主题发一条模拟报文，目标板串口打印出识别日志 |
| 负责人 | 吴遂杰（协议层维护人） |
| 为什么在这个位置 | 阶段 4 和阶段 5 都要用它；先集中做一次，避免每个模块各自造轮子 |

**为什么要上报事件而不是静默执行**：风扇被联动打开时，平台和语音板都需要知道"这是联动触发的"，否则语音板只能靠猜。上报一条扩展 evt 后，平台界面上能看到原因，语音板也能播报"温度过高，已自动开启风扇"。这批事件属于协议 8.6 节定义的扩展数据点范畴，格式与表 20 一致。

---

### 阶段 4 · 四条联动规则（任务 1）

| 项目 | 内容 |
|---|---|
| 目标 | 四条规则全部在板端闭环，不依赖平台 |
| 改动 | 1 号板（舵机）、2 号板（风扇）、5 号板（RGB+蜂鸣器）各加一个 `linkage.h/.cpp`，逻辑与语音板的 `linkage.cpp` 同构 |
| 产出 | 传感器一变，执行器就动，延时在 1 秒以内 |
| 验收 | 逐条演示：加热/吹气 → 风扇转；挡光路 → 舵机转；遮光 → 灯光亮；打火机 → 彩灯循环 + 蜂鸣器响 |
| 负责人 | 见第四章的表 |
| 为什么在这个位置 | 依赖阶段 1 的统一命名与阶段 3 的通道；且必须在语音重设计之前，因为语音要播报这些联动事件 |

详细设计见第四章。

---

### 阶段 5 · 智能语音重新设计（任务 5）

| 项目 | 内容 |
|---|---|
| 目标 | 让喇叭既能及时报紧急事件，又能周期性把全部模块的数据都念一遍 |
| 改动 | `智能语音/src/linkage.cpp` 重构为三层播报；`tts.h/.cpp` 的队列扩容与去重；`my_config.h` 新增汇总周期宏 |
| 产出 | 双轨播报：事件即时播 + 数据周期汇总播 |
| 验收 | 八块板同时上电，连续观察 5 分钟：告警不漏、汇总不卡、喇叭不刷屏 |
| 负责人 | 吴遂杰（7 号板原作者） |
| 为什么在这个位置 | 依赖阶段 4 上报的联动事件；且它需要反复试听调参，放在最后留足时间 |

详细设计见第六章。

---

### 阶段 6 · 全量编译验证 + 八板联调

| 项目 | 内容 |
|---|---|
| 目标 | 八个工程编译通过、八块板上电互不干扰、四条联动与语音播报全部正常 |
| 方法 | ① 八个工程依次 `pio run`；② 用 MQTTX 订阅 `202609_PP2/#` 观察全部报文；③ 按第八章的演示脚本走一遍 |
| 验收 | 演示脚本全部通过，串口无 `fail`、无重启 |
| 负责人 | 全员，组长主持 |

---

## 三、统一命名规范（任务 2 的验收标准）

### 3.1 配置文件 `my_config.h`

所有板完全一致的段落：

```c
// ---- 网络与服务器（协议附录 A）----
#define WIFI_SSID     "..."              // 注意：不能叫 SSID，会与 WiFi.h 冲突
#define WIFI_PASSWORD "..."              // 注意：不能叫 PASSWORD
#define MQTT_SERVER   "yunyismart.tech"  // 原 mqttServer / MQTT_HOST
#define MQTT_PORT     1883               // 原 mqttPort

// ---- 本组编号与主题 ----
#define GROUP_ID      1
#define TOPIC_CMD     "202609_PP2/1/cmd"
#define TOPIC_REPORT  "202609_PP2/1/report"
#define TOPIC_ONLINE  "202609_PP2/1/online"

// ---- 本板身份（协议表 A-1）----
#define BOARD_ID      1                  // 原 ID，改名为 BOARD_ID（ID 太通用）
#define BOARD_ID_STR  "1"                // 原 SRC，避免每次现算 String(BOARD_ID)

// ---- 外设使能（协议表 A-1）----
#define HAS_SERVO     1                  // 按板号只留对应的一行

// ---- 设备标识（协议表 7）----
#define DEV_SERVO     "servo"

// ---- 协议层开关 ----
#define ACK_AFTER_DONE    0              // 执行器做完动作才回执
#define SUB_EXTRA_REPORT  0              // 是否旁听 report（联动板为 1）
#define SUB_EXTRA_ONLINE  0              // 是否旁听 online
#define MQTT_BUF_SIZE     1024           // 必须放大，默认 128 会静默截断
```

`MQTT_SERVER` / `MQTT_PORT` / `BOARD_ID` / `BOARD_ID_STR` 这四个改名要全局搜索替换，`mqttServer`、`mqttPort`、`ID`、`SRC` 在代码里还有引用。

### 3.2 函数命名

| 类别 | 规范 | 示例 |
|---|---|---|
| 模块生命周期 | `<模块>_init()` / `<模块>_loop()` | `servo_init()`、`fan_loop()` |
| 模块动作 | `<模块>_<动词>()` | `servo_set_angle()`、`fan_set_power()`、`rgb_apply()`、`buzzer_set_power()` |
| 模块状态查询 | `<模块>_<名词>()` | `servo_busy()`、`servo_angle()`、`buzzer_busy()` |
| 协议层 | `proto_<动词>()` | `proto_init()`、`proto_send_dat()`、`proto_send_evt()` |
| 协议层钩子 | `device_busy()` / `proto_on_other()` | 弱符号，各板按需覆盖 |
| 联动层 | `linkage_<动词>()` | `linkage_on_message()`、`linkage_loop()` |
| 命令分发 | `handle_device_cmd()` | 各板 `main.cpp` 实现，签名完全一致 |

模块名一律用下划线小写：`ir_beam`、`light_sensor`、`fan`、`flame`、`sht30`、`rgb`、`buzzer`、`adc`、`servo`、`tts`。

### 3.3 变量命名

| 类别 | 规范 | 示例 |
|---|---|---|
| 全局配置 | 宏，全大写下划线 | `LED1_PIN` |
| 文件内静态变量 | `snake_case`，加模块前缀 | `static uint32_t last_report_ms;` |
| 协议层私有状态 | `tx_seq` / `pend_*` / `wait_*` | 保持 A 套现状 |
| 布尔量 | `is_` / `has_` 前缀或 `_ok` 后缀 | `is_blocked`、`flame_state` |

**统一后要删除的旧名字**：`SSID`、`PASSWORD`、`WIFI_PASS`、`MQTT_HOST`、`MQTT_USER`、`MQTT_PASS`、`mqttServer`、`mqttPort`、`ID`、`SRC`、`espClient`、`wifiClient`、`BOARD_ID_STR` 以外的序号别名。

**MQTT 用户名一律不要**：删掉 `MQTT_USER` / `MQTT_PASS`，改为匿名连接，与其余六块板一致，符合协议表 21。

---

## 四、四条联动规则设计（任务 1）

### 4.1 总体架构：边缘联动

四条规则**全部在板端闭环**，不经过平台：

```
传感器板（3/4/6/8）──上报 report──┐
                                 ↓
                     执行器板自己订阅 report
                     自己判断阈值 → 直接驱动本地执行器
                                 ↓
                     上报一条扩展 evt（让平台/语音板知道）
```

**为什么不走"发 cmd 给自己"的中转**：

- 板端直接调用本地函数，没有 MQTT 往返，**延时最小**（联动要在 1 秒内可见）
- 少一次收发，就少一次自回环、重复投递、消息乱序的风险
- 平台没写好、网络抖动时联动照样工作（协议第七章的联动是"由平台编排"，本组在板端也做一遍，属于鲁棒性增强，两者不冲突）

**代价与对策**：多块板订阅了 report，与协议 8.4 节「各板不得订阅 report」的约定有偏离。对策与语音板已有的做法完全一致——回调第一件事就是 `if (atoi(src) == BOARD_ID) return;` 丢掉自己的报文，自回环在物理上不可能形成；并且这个偏离只影响订阅范围，主题数量、信封字段、报文类型一个都没动。语音板已经用这个方案跑了很久，属于成熟做法。

**谁订阅 report**：

| 板 | 订阅原因 |
|---|---|
| 1 号 servo | 听 3 号 ir_beam 的遮挡事件 |
| 2 号 fan | 听 4 号 sht30 的温湿度 |
| 5 号 rgb/buzzer | 听 8 号 light_sensor 的光照、6 号 flame 的火焰事件 |
| 7 号 tts | 播报全部模块（原有功能） |

3、4、6、8 号板是数据源，**不订阅 report**，只上报。

### 4.2 规则 1：温湿度过高 → 风扇（2 号板）

| 项目 | 内容 |
|---|---|
| 触发源 | 4 号板 sht30 的 dat 报文 |
| 判定 | `temperature >= TEMP_FAN_ON`（建议 30.0 ℃）或 `humidity >= HUMI_FAN_ON`（建议 80.0 %RH） |
| 动作 | `fan_set_power(true)` |
| 关闭条件 | 温度与湿度**都**回到 `TEMP_FAN_OFF`（建议 28.0）/ `HUMI_FAN_OFF`（建议 75.0）以下，持续 `FAN_HOLD_MS`（建议 10 秒） |
| 迟滞 | 开启阈值与关闭阈值必须分开，否则在阈值附近会反复开关，继电器啪啪响 |
| 上报 | `evt {device:"fan", event:"auto_on"/"auto_off", level:1/0, reason:"temperature"/"humidity"}` |
| 负责人 | 施昱名 |

**保护逻辑**：若平台已经手动开过风扇，联动不再重复开；联动的关闭动作不得覆盖平台的手动开启。做法是记录 `manual_override` 标记，平台命令优先级最高。

### 4.3 规则 2：对射遮挡 → 舵机（1 号板）

| 项目 | 内容 |
|---|---|
| 触发源 | 3 号板 ir_beam 的 evt 报文 |
| 判定 | `event == "blocked"` / `event == "clear"` |
| 动作 | `servo_set_angle(LINK_SERVO_BLOCKED_DEG)`（建议 0°）；`clear` 时回到 `LINK_SERVO_CLEAR_DEG`（建议 90°） |
| 防抖 | 3 号板本身已做 50 ms 去抖 + 500 ms 限流，联动的动作间隔再加 `LINK_SERVO_MIN_GAP_MS`（建议 1000 毫秒）保护舵机 |
| 上报 | `evt {device:"servo", event:"auto_move"/"auto_home", level:1/0}` |
| 负责人 | 吴遂杰 |

舵机走平滑转动状态机，联动调用只设目标角度、立即返回，不阻塞主循环（复用现有 `servo_set_angle()`，零改动）。

### 4.4 规则 3：光照过暗 → RGB 照明（5 号板）

| 项目 | 内容 |
|---|---|
| 触发源 | 8 号板 light_sensor 的 dat 报文 |
| 判定 | `illuminance <= LIGHT_DARK_LUX`（建议 50 lx） |
| 动作 | `rgb_apply(255, 200, 120)` —— 一种暖白色，用作照明 |
| 关闭条件 | `illuminance >= LIGHT_BRIGHT_LUX`（建议 120 lx），迟滞同理 |
| 上报 | `evt {device:"rgb", event:"scene_light"/"scene_off", level:1/0}` |
| 负责人 | 吴一凡 |

### 4.5 规则 4：火焰 → RGB 报警 + 蜂鸣器（5 号板）

| 项目 | 内容 |
|---|---|
| 触发源 | 6 号板 flame 的 evt 报文 |
| 判定 | `event == "detected"` / `event == "clear"` |
| 动作 | RGB 多彩循环（红→橙→黄→红，每色 `FIRE_STEP_MS` 建议 300 毫秒）+ 蜂鸣器间歇鸣叫（响 300 毫秒、停 300 毫秒循环） |
| 恢复 | `clear` 后熄灭 RGB、关蜂鸣器，并重新按当前光照状态决定是否进入照明场景 |
| 上报 | `evt {device:"rgb", event:"scene_fire"/"scene_off", level:3/0}` + `evt {device:"buzzer", event:"alarm_on"/"alarm_off", level:3/0}` |
| 负责人 | 吴一凡 |

RGB 的多彩循环需要**按帧刷新**，所以 5 号板要新增 `rgb_loop()`（现有代码注释里写着"没有需要按帧刷新的东西，所以没有 rgb_loop()"，这次要补上）。蜂鸣器的间歇鸣叫同理，放在 `buzzer_loop()` 里。

### 4.6 5 号板的显示仲裁（两条规则抢同一组灯）

光照照明和火焰报警都要控制 RGB 和蜂鸣器，必须有明确的优先级，否则两条规则会互相覆盖：

| 优先级 | 来源 | 行为 |
|---|---|---|
| 3（最高） | 火焰告警 | 多彩循环 + 蜂鸣器；**期间抑制光照照明** |
| 2 | 平台手动命令 | 立即生效，并置 `manual_override` 标记 |
| 1（最低） | 光照联动 | 仅在无更高优先级时生效 |

**状态重算模型**（不要用"一个事件改一次灯"的写法）：

- 5 号板维护一个"当前场景"变量：`SCENE_FIRE` / `SCENE_LIGHT` / `SCENE_OFF`
- 任何输入变化（火焰开始/结束、光照跨阈值、平台命令）都只是改变输入状态，然后调用 `scene_update()` 重算一次场景
- 重算规则：火焰激活 → `SCENE_FIRE`；否则光照过暗且无 `manual_override` → `SCENE_LIGHT`；否则 → `SCENE_OFF`
- `manual_override` 在下一次传感器状态**变化**时自动清除

这样无论事件以什么顺序到达，最终状态都是一致的，不会出现"火焰灭了但灯还是红的"或"平台设了蓝色但被光照规则立刻盖掉"。

### 4.7 防自回环与防抖（所有联动板共有）

1. **丢弃自己发的报文**：`if (atoi(src) == BOARD_ID) return;` —— 协议层已有，联动板直接继承
2. **只在状态跳变时动作**：收到的每条 sht30 dat 都是"当前值"，但只有跨越阈值的**那一次**才动作，其余只是更新快照
3. **阈值迟滞**：开/关阈值分开（见 4.2）
4. **动作限流**：每条规则一个最小动作间隔，防止传感器抖动导致执行器反复动作
5. **联动不上报 dat**：只上报 evt，避免增加 report 主题的周期流量

---

## 五、蜂鸣器实现（任务 3）

### 5.1 协议要求

| 来源 | 报文 |
|---|---|
| 表 12（必须实现） | 下行 `{"device":"buzzer","action":"set_power","param":{"on":true}}`，ack 回发 `param:{"on":true}` |
| 表 20（选做） | 下行 `{"device":"buzzer","action":"beep","param":{"duration":500}}`，时长 50～5000 毫秒 |

两个都做。`beep` 是不限时长的加分项，实现成本极低。

### 5.2 实现要点

- **非阻塞**：`beep` 的最长 5000 毫秒必须用"记下该停的时刻 + `buzzer_loop()` 每轮检查"的方式，绝不能 `delay()`
- **与 RGB 共存的调度**：5 号板的 `loop()` 要同时驱动 `proto_loop()`、`rgb_loop()`（火焰场景的帧刷新）、`buzzer_loop()`
- **间歇鸣叫**：火焰报警要求响 300 毫秒停 300 毫秒，这属于"报警模式"，与 `set_power` 的持续鸣叫是两种状态，用一个状态机统管，避免两个来源抢控制权
- **使能宏**：`HAS_BUZZER` 由 0 改为 1；无源蜂鸣器要用 LEDC 输出方波，有源蜂鸣器只需 GPIO 高低电平——**按实验箱实际情况二选一**，代码里用一个宏 `BUZZER_ACTIVE_LEVEL` 与 `BUZZER_USE_PWM` 切换

### 5.3 待确认硬件项（动手前必须落实）

| 项 | 现状 | 怎么办 |
|---|---|---|
| 蜂鸣器 GPIO | 代码里没有记录，原理图文件名是《SCH_RGB蜂鸣器电位器_2025-09-23》 | 查原理图或万用表实测，填进 `BUZZER_PIN` |
| 有源 / 无源 | 未知 | 有源：GPIO 直接驱动；无源：LEDC 输出 2～4 kHz 方波 |
| 电位器（ADC）引脚 | 未知，本次**不做**，`HAS_ADC` 保持 0 | 不影响本任务 |

---

## 六、智能语音重新设计（任务 5）

### 6.1 现有实现为什么不够

现有 `linkage.cpp` 的规则表只覆盖了**越阈值的异常**（温度过高、湿度过高、光线过暗）和**状态事件**（火焰、遮挡、上下线、舵机）。正常范围内的数据一条都不播。用户的感受是"不能全部播报全部的信息"，指的就是这个。

**更根本的矛盾是速度不匹配**：

| 统计项 | 数值 |
|---|---|
| 稳态报文产生速率 | 4 号板 0.2 条/秒 + 6 号板 1 条/秒 + 8 号板 0.5 条/秒 ≈ **1.7 条/秒**（若 ADC 启用则约 2.7 条/秒） |
| 即每分钟 | 约 **100～160 条**报文 |
| 语音播报能力 | 一句话含间隔约 2.5 秒，每分钟最多 **20～24 句** |
| 比值 | **数据产生速度是播报能力的 5～7 倍** |

**结论：逐条播报在物理上不可能。** 就算把队列开到无限大，播报也会越积越滞后，几分钟后就变成"念着两分钟前的数据"，毫无意义。正确的做法不是加大队列，而是**在板内做信息压缩**——把多个模块的数据合并成一句话，按固定周期播报。

### 6.2 新设计：三层播报

```
第一层  事件即时播报（evt / sys / ack）        最高优先，状态一变立刻播
       ├─ 火焰 detected / clear              （含 4.5 的联动事件）
       ├─ 对射 blocked / clear               （含 4.3 的联动事件）
       ├─ 风扇自动开 / 关                     ← 新增，来自阶段 4 的 evt
       ├─ 舵机转向 / 复位                     ← 新增
       ├─ RGB / 蜂鸣器报警场景切换             ← 新增
       ├─ 设备上线 / 离线
       └─ 平台下发的播报命令
                    ↓
第二层  阈值告警（dat 越界）                    中优先，带冷却
       ├─ 温度 ≥ 30.0   "温度过高，当前 32.5 度"
       ├─ 湿度 ≥ 80.0   "湿度偏高，当前 85%"
       └─ 光照 < 50     "光线过暗，请注意"
                    ↓
第三层  周期数据汇总（dat 快照）                最低优先，空闲才播
       └─ 每 SUMMARY_PERIOD_MS（建议 30 秒）把全部模块的最新值念一遍
```

**第三层是本次新增的核心**，它解决"全部信息都要播报"的诉求：

- 语音板为每个数据源维护一份**最新值快照**（不是队列！），新数据来了就覆盖旧的
- 每 30 秒把快照拼成**一句话**播报一次
- 因为快照只保留最新值，无论数据来得多快，播报的永远是当前值，不会滞后
- 汇总播报优先级最低：正在播告警时不插嘴，告警排队也不插队，只在空闲时播

### 6.3 汇总播报的文本模板

```c
// 环境汇总：温度 26.5 度，湿度 60%，光照 125 勒克斯，对射通畅，火焰正常
```

| 数据源 | 快照键 | 播报措辞 | 缺省值（还没收到数据时） |
|---|---|---|---|
| 4 号 sht30 | `temperature` | "温度26.5度" | "温度未知" |
| 4 号 sht30 | `humidity` | "湿度60%" | 并入上一项 |
| 8 号 light_sensor | `illuminance` | "光照125勒克斯" | "光照未知" |
| 3 号 ir_beam | 状态 | "对射通畅" / "对射遮挡" | "对射未知" |
| 6 号 flame | 状态 | "火焰正常" / "有火焰" | "火焰未知" |
| 5 号 adc（若启用） | `raw` | "电位器2048" | 该项省略 |

**长度核算**：上面整句约 25 个汉字 + 若干数字，GB2312 编码后约 60 字节，小于协议 §5.7 建议的 100 字节上限，安全。

**分段策略**：若某个模块长期无数据（比如某块板没上电），对应片段降级为"未知"而不是整句不播——这样用户始终能听到完整的一句话，而不会因为一块板掉线就听不到任何汇总。

### 6.4 队列与优先级的调整

| 参数 | 现值 | 新值 | 理由 |
|---|---|---|---|
| `TTS_QUEUE_LEN` | 3 | 6 | 事件变多了（新增四条联动的联动事件），3 条太容易挤爆 |
| 冷却档位 | 告警 30 秒 / 提示 60 秒 | 不变 | 已经够用 |
| 汇总播报 | 无 | 新增，30 秒周期，等级 1（提示） | 空闲才播 |
| 汇总去重 | 无 | 与周期绑定，天然不重复 | — |
| 队列清空策略 | 满则丢最低等级 | 不变 | 合理 |

**关键约束**：汇总播报**不进入队列**。它是"到点了且现在空闲就播，不空闲就跳过这一轮"。若进队列，告警一多就会排到几分钟后，那时数据已经过期了——跳过比滞留更正确。

### 6.5 需要保留的现有设计（不要推倒重来）

现有 `linkage.cpp` 里有几处是踩过坑才写出来的，重构时必须原样保留：

- **火焰按轮次播报**：`flame_alarming` 标记 + `FLAME_RESEND_GAP_MS` 兜底。6 号板每 5 秒重发 detected，逐条播会把喇叭卡在同一句话上
- **告警与解除配对**：本轮播过告警才播解除，避免抖动时来回念
- **开机静默期** `ONLINE_MUTE_MS`：online 主题是 Retain 的，不屏蔽会开机念一串"X 号设备已上线"
- **`src` 过滤**：自己的报文一律不播
- **回调里只入队不发送**：arduino-mqtt 的回调里 publish 会死锁，串口下发必须留在 `tts_loop()`

---

## 七、组员分工

改造原则：**谁的模块谁改，谁的板谁烧**。协议层与联动框架由组长统一提供，各人只改自己模块的业务逻辑。

| 成员 | 负责板 | 阶段 1（命名归一） | 阶段 2-4 | 阶段 5 | 阶段 6 |
|---|---|---|---|---|---|
| 吴遂杰 | 1 号 servo、7 号 tts | 舵机/智能语音工程命名对齐；提供协议层模板 | 规则 2（对射→舵机） | **主责**：语音三层播报重构 | 主持联调 |
| 吴一凡 | 4 号 sht30、5 号 rgb | sht30/rgb 工程命名对齐 | **规则 3、4**（光照→照明、火焰→报警+蜂鸣器）+ 蜂鸣器实现 | 配合提供 5 号板事件 | 5 号板烧录验证 |
| 李豪 | 3 号 ir_beam、8 号 light_sensor | **3、8 号板从 PubSubClient 迁移到 256dpi/MQTT**，改为多文件结构 | 保证 evt/dat 上报格式不变 | — | 3、8 号板烧录验证 |
| 施昱名 | 2 号 fan、6 号 flame | **2、6 号板从 PubSubClient 迁移到 256dpi/MQTT**，改为多文件结构；删掉多余的 MQTT 用户名 | **规则 1**（温湿度→风扇） | — | 2、6 号板烧录验证 |

**跨人协作的两个约定**：

1. `mqtt_proto.h/.cpp` 是四份（迁移后八份）副本，改一份必须同步全部。同步后这样自检：
   ```bash
   diff 舵机/src/mqtt_proto.cpp 智能语音/src/mqtt_proto.cpp
   ```
   阶段 3 的协议层扩展做完后，八个工程必须全部重新同步一次。
2. 联动阈值统一写在各自的 `my_config.h` 里，**不在代码里写魔数**。现场演示时阈值要能快速调整。

---

## 八、风险清单与待确认项

### 8.1 待确认的硬件项（动手前必须落实）

| 项 | 影响 | 责任人 |
|---|---|---|
| 蜂鸣器 GPIO 与有源/无源 | 阶段 2 无法开始 | 吴一凡 |
| 5 号板电位器（ADC）引脚 | 本任务不做，`HAS_ADC` 保持 0 | 吴一凡 |
| 舵机独立 5V 供电与共地 | 舵机不动 / ESP32 被拉复位 | 吴遂杰 |
| 语音模块 8E1 与 115200 | 写错就是"完全没声音且不报错" | 吴遂杰 |
| WiFi 热点（当前是个人热点） | 教室现场能否连上 | 全员 |

### 8.2 技术风险

| 风险 | 概率 | 对策 |
|---|---|---|
| 迁移 4 块板到新库时引入回归 | 中 | 阶段 1 只改结构不改逻辑，逐板用 MQTTX 按附录 B 回归一遍再提交 |
| 八块板同时上线导致 report 主题拥塞 | 低 | 联动只上报 evt，不增加 dat；若仍拥塞，把语音板的汇总周期调长 |
| 多块板订阅 report 加重服务器负担 | 低 | 4 块板订阅同一主题，对 MQTT 服务器而言是常规负载 |
| 语音播报打断演示节奏 | 中 | 所有阈值、周期、冷却都是 `my_config.h` 里的宏，现场可即时调整 |
| 舵机与火焰同时联动时电流冲击 | 中 | 舵机与语音模块各自独立 5V 供电，绝不从 ESP32 取电 |

### 8.3 协议符合性说明（评审时可能被问到）

本组在协议之外做了两处扩展，都要能说清楚：

1. **多块板订阅 report**：协议 8.4 节要求各板不订阅 report，本组有 4 块板订阅了。理由是板端联动需要旁听；自回环风险已由 `src` 过滤彻底堵死；可通过 `SUB_EXTRA_REPORT=0` 一键关闭，退回协议标准形态。
2. **扩展事件**（`auto_on`、`scene_fire` 等）：属于协议 8.6 节允许的"扩展数据点"，格式与表 20 一致，不影响标准报文的解析。

---

## 九、Git 版本管理约定

### 9.1 仓库

- 远端：GitHub 公开仓库（阶段 0 已建立）
- 忽略：`.pio/`（每个约 37 MB 的构建产物）、`.vscode/`、`.deep-copilot/`

### 9.2 分支策略

| 分支 | 用途 |
|---|---|
| `main` | 随时可演示的稳定版本，只接受验证通过的合并 |
| `stage/1-rename` | 阶段 1 |
| `stage/2-buzzer` | 阶段 2 |
| `stage/3-proto` | 阶段 3 |
| `stage/4-linkage` | 阶段 4 |
| `stage/5-tts` | 阶段 5 |

一个阶段一个分支，做完、验证通过再合回 `main`。这样任何一个阶段出问题都能单独回退，不会连带毁掉其他模块。

### 9.3 提交信息规范

```
<阶段号> <模块>: <做了什么>

例：
stage1 servo: 统一命名 WIFI_SSID/BOARD_ID，功能不变
stage2 rgb: 新增蜂鸣器 set_power 与 beep
stage4 fan: 温湿度联动，含迟滞与手动优先
```

### 9.4 每人提交前必须做

1. `pio run` 编译自己负责的工程，确认零错误
2. 按协议附录 B 的报文回归一遍原有功能，确认行为没变
3. 只在 `stage/*` 分支提交，验证通过后由组长合并

---

## 附：执行进度

- [x] 通读 8 个模块源码、4 份成员 readme、协议规范 V2.0
- [x] 识别出两套技术栈的差异与四个阻塞问题
- [x] **阶段 0 · 基线备份**：GitHub 公开仓库 + 基线提交
- [x] **阶段 1 · 命名统一与工程结构归一** —— 代码完成，编译验证通过
  - 八个工程全部 `pio run` SUCCESS
  - 八份 `mqtt_proto.cpp/.h` 副本逐字节一致
  - 全仓库已无 `mqttServer` / `mqttPort` / `MQTT_USER` / `PubSubClient` 的实际引用
  - **待办（需要硬件）**：逐块烧录，用 MQTTX 按协议附录 B 回归原有功能，
    确认改造前后行为一致
- [ ] 阶段 2 · 蜂鸣器实现
- [ ] 阶段 3 · 协议层扩展
- [ ] 阶段 4 · 四条联动规则
- [ ] 阶段 5 · 智能语音重新设计
- [ ] 阶段 6 · 全量联调

### 阶段 1 执行中的三处计划外调整

记录在此，便于回溯：

1. **`proto_send_evt()` 提前到阶段 1 实现**。原计划放在阶段 3，但 3、6 号板
   迁移后就需要发事件，否则没有可用的上报接口。函数定义在协议层，
   八份副本同步，阶段 3 剩余的部分（订阅扩展、联动上报）不受影响。
2. **客户端编号统一为 `esp32-板号-MAC`**。协议 §8.4 建议的格式，原来 4 块
   单文件板用这个格式、4 块多文件板只用 MAC。统一后八板一致。
3. **6 号板 dat 周期由 2000 ms 改为 1000 ms**。协议表 19 要求 1 秒，
   原作者 readme 也写的是"每秒采集一次"，代码里的 2000 是实现笔误。

### 当前阻塞

**阶段 2（蜂鸣器）无法开工**，需要先确认第八章 8.1 节的硬件信息：

- 蜂鸣器接在哪个 GPIO
- 是有源蜂鸣器（GPIO 电平直接驱动）还是无源蜂鸣器（需要 PWM 方波）

阶段 3～5 不依赖这一项，可以先做。
