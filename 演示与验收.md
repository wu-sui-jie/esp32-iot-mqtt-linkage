# 演示与验收步骤

> 按这个顺序走一遍，可以确认八个模块的原有功能没有被改造破坏，
> 四条联动与语音播报都正常工作。
> 协议依据：《MQTT 通信协议规范_第1组》V2.0

---

## 〇、烧录前必须先做的两件事

### 1. 确认蜂鸣器引脚（**必做**）

`rgb_board5/src/my_config.h` 里的这一行目前是**占位值**：

```c
#define BUZZER_PIN 26 // ← 待确认
```

请对照原理图《SCH_RGB蜂鸣器电位器_2025-09-23》或万用表实测，
改成实际引脚再烧录。引脚填错不会损坏器件，但蜂鸣器不会有任何反应。

同时确认是有源还是无源蜂鸣器：

- 无源（需要方波驱动）：保持 `BUZZER_USE_PWM 1`
- 有源（给电平就响）：若烧录后声音发闷或很小，改成 `BUZZER_USE_PWM 0`

### 2. 确认 WiFi 热点

八个工程的 `my_config.h` 里目前都是：

```c
#define WIFI_SSID     "wusuijie"
#define WIFI_PASSWORD "11111111"
```

教室现场若不是这个热点，八个文件都要改（这是本次没有统一的一项）。

---

## 一、单板验证：原有功能没有被破坏

用 MQTTX 连上 `yunyismart.tech:1883`（用户名密码留空），
订阅 `202609_PP2/#`，发布主题 `202609_PP2/1/cmd`，QoS 1，不勾 Retain。

### 1 号板 舵机

| 发这条 | 应该看到 |
|---|---|
| `{"ver":"1.0","type":"cmd","seq":1,"src":"web","dst":1,"body":{"device":"servo","action":"set_angle","param":{"angle":180}}}` | 舵机平滑转过去（约 1.8 秒），到位后才回 ack，param 里 angle 是 180 |
| `...{"angle":200}` | 舵机**不动**，ack 的 result 是 `fail` |
| `...{"angle":90.5}` | 舵机不动，ack 是 `fail` |

### 2 号板 风扇

| 发这条 | 应该看到 |
|---|---|
| `{"ver":"1.0","type":"cmd","seq":2,"src":"web","dst":2,"body":{"device":"fan","action":"set_power","param":{"on":true}}}` | 风扇转，ack 里 `param.on` 是 `true` |
| `...{"on":false}` | 风扇停 |
| 不带 param（`"body":{"device":"fan","action":"set_power"}`） | 风扇**保持原状态**，ack 是 `fail` |

### 4 号板 温湿度

不用发命令，上电就每 5 秒一条 dat，看订阅区的小数是不是 1 位。

### 5 号板 RGB 与蜂鸣器

| 发这条 | 应该看到 |
|---|---|
| `{"ver":"1.0","type":"cmd","seq":3,"src":"web","dst":5,"body":{"device":"rgb","action":"set_color","param":{"r":255,"g":0,"b":0}}}` | 四颗灯全变红，ack 回发同样的颜色 |
| `...{"r":0,"g":0,"b":0}` | 灯熄灭 |
| `...{"r":300,"g":0,"b":0}` | 灯**不动**，ack 是 `fail` |
| `{"ver":"1.0","type":"cmd","seq":5,"src":"web","dst":5,"body":{"device":"buzzer","action":"set_power","param":{"on":true}}}` | 蜂鸣器持续响，ack 回 `param.on = true` |
| `...{"on":false}` | 停 |
| `{"...","action":"beep","param":{"duration":500}}` | 响 0.5 秒后**自动停** |
| `...{"duration":9999}` | 不响，ack 是 `fail`（超出协议规定的 50~5000） |

### 7 号板 语音（平台下发）

| 发这条 | 应该听到 |
|---|---|
| `{"ver":"1.0","type":"cmd","seq":6,"src":"web","dst":7,"body":{"device":"tts","action":"play_text","param":{"text":"欢迎使用物联网系统"}}}` | 播报「欢迎使用物联网系统」，ack 立即回（不等播完） |

### 3、6、8 号板

纯传感器，不用发命令：

- 3 号板：遮住光路应立即上报 `evt blocked`，移开上报 `evt clear`，
  状态不变时每 30 秒一条心跳
- 6 号板：**开机 2 秒内不要点火**（在校准基线），之后打火机靠近应
  上报 `evt detected`，强度 dat 每 1 秒一条
- 8 号板：光照度 dat 每 2 秒一条

---

## 二、四条联动的验证

每条都有两种测法：**用真实传感器**（演示时用）和
**用 MQTTX 模拟**（没硬件时调试用）。

模拟时注意：向 `202609_PP2/1/report` 发布，`src` 必须是被模拟板的板号
（协议 §9.3），否则会被当成别的板发的。

### 联动 1：温湿度过高 → 2 号板自动开风扇

| 怎么造条件 | 期望现象 |
|---|---|
| **真实**：用手捂住 4 号板的 SHT30 传感器（体温加热，约 1~2 分钟）；或对传感器哈气提高湿度 | 风扇转动；串口打印「温湿度过高，联动开启风扇」；订阅区出现 `evt auto_on`；语音播报「环境超标，已自动开启风扇」 |
| **模拟**：发 `{"ver":"1.0","type":"dat","seq":88,"src":"4","dst":0,"body":{"device":"sht30","samples":[{"key":"temperature","value":32.5},{"key":"humidity","value":60.2}]}}` | 同上 |

恢复：温度回落到 28 ℃ 以下并持续 10 秒后风扇才停（迟滞设计，见下）。

> **为什么不是立刻停**：如果开关用同一个阈值，温度在 30 ℃ 上下浮动时
> 继电器会一秒内吸合好几次，既吵又伤触点。所以开阈值 30、关阈值 28，
> 中间 2 ℃ 是缓冲区；而且条件恢复后要连续正常 10 秒才关。

### 联动 2：对射遮挡 → 1 号板舵机转动

| 怎么造条件 | 期望现象 |
|---|---|
| **真实**：用手或纸板挡住 3 号板红外对射的光路 | 舵机转到 0°；语音播报「注意，检测到遮挡」+「舵机已转到遮挡位」 |
| 移开遮挡物 | 舵机回到 90°；语音「遮挡已恢复」+「舵机已复位」 |
| **模拟**：发到 report，`{"ver":"1.0","type":"evt","seq":58,"src":"3","dst":0,"body":{"device":"ir_beam","event":"blocked","level":2}}` | 舵机转到 0° |

> 3 号板每 30 秒会重发一次心跳 evt（协议 5.3 节要求），
> 那不会让舵机重复转动——1 号板只在状态跳变时才动作。

### 联动 3：光照过暗 → 5 号板 RGB 亮灯照明

| 怎么造条件 | 期望现象 |
|---|---|
| **真实**：用手捂住 8 号板的光照传感器 | RGB 亮暖白光（r=255 g=200 b=120）；语音「光线过暗，请注意」+「光线过暗，已开启照明」 |
| 用手机手电筒照传感器 | 光照升到 120 lx 以上时 RGB 熄灭 |
| **模拟**：发 `{"ver":"1.0","type":"dat","seq":76,"src":"8","dst":0,"body":{"device":"light_sensor","samples":[{"key":"illuminance","value":20.0}]}}` | RGB 亮暖白光 |

### 联动 4：火焰 → 5 号板多彩循环 + 蜂鸣器报警

| 怎么造条件 | 期望现象 |
|---|---|
| **真实**：打火机在 6 号板火焰传感器前 10~20 cm 处打火（**注意：6 号板开机 2 秒内不要有火**） | RGB 在红→橙→黄之间循环（每 0.3 秒一色）；蜂鸣器响 0.3 秒停 0.3 秒循环；语音「警告，检测到火焰」 |
| 移开打火机 | RGB 熄灭（或按当前光照转成照明），蜂鸣器停；语音「火焰警报已解除」 |
| **模拟**：发 `{"ver":"1.0","type":"evt","seq":31,"src":"6","dst":0,"body":{"device":"flame","event":"detected","level":3}}` | 同上 |

> **演示要点**：灭火后**立刻**再点火，应该立即播报，不会被冷却压住。
> 如果这条不响，说明 `COOLDOWN_FLAME_MS` 被改大了（默认 3 秒）。

### 两条规则抢同一组灯时谁赢

5 号板同时管照明和火焰报警，优先级是：

```
火焰报警（3）  >  平台手动命令（2）  >  光照照明（1）
```

现场验证这两个场景：

1. **火焰优先**：先让光照变暗（灯亮暖白），再打火 →
   灯应该立刻改成多彩循环 + 蜂鸣器响；灭火后自动回到照明状态
2. **平台优先**：让光照变暗（灯亮暖白），然后用 MQTTX 发
   `set_color{r:0,g:0,b:255}` → 灯变蓝并**保持**，不会被光照联动改回暖白；
   等下一次光照状态变化（比如用手电照一下再松开）联动才恢复

---

## 三、语音播报的验证

三层播报各自的测法：

### 第一层 事件即时播报

按第二章的四条联动各做一遍，每次都应该有对应的播报。

### 第二层 阈值告警

| 怎么造条件 | 应该听到 |
|---|---|
| 温度升到 30 ℃ 以上 | 「温度过高，当前 XX.X 度」 |
| 湿度升到 80% 以上 | 「湿度偏高，当前 XX%」 |
| 遮住光照传感器 | 「光线过暗，请注意」 |

### 第三层 周期数据汇总（本次新增）

**等 30 秒左右**，在没有告警播报的空闲时刻，应该听到一句：

> 「环境汇总，温度26.5度，湿度60%，光照125勒克斯，对射通畅，火焰正常」

验证要点：

1. **内容完整**：四块传感器板（3、4、6、8）的数据都应该出现在句子里
2. **值是当前的**：一边听一边看 MQTTX 里各板的实时上报，两者应该对得上
3. **不刷屏**：每 30 秒才一句，告警播报期间它会自动让路
4. **掉一块板也能听完整句**：把 8 号板断电，等下一轮汇总，
   句子变成「环境汇总，温度26.5度，湿度60%，对射通畅，火焰正常」——
   光照那一段整段省略，而不是念「光照未知」

想把汇总关掉或改周期，改 `智能语音/src/my_config.h`：

```c
#define TTS_SUMMARY_ENABLE 1        // 改成 0 就完全不播汇总
#define SUMMARY_PERIOD_MS 30000UL   // 改小会播得更勤
```

### 抢占测试

趁它正在念「光线过暗」时立刻打火点一下 →
应该立即打断，改念「警告，检测到火焰」。
（平台下发的命令等级最高，任何时候都能盖过本地联动播报。）

---

## 四、广播与遗嘱

| 测试 | 怎么做 | 应该看到 |
|---|---|---|
| 广播 | 发 `{"ver":"1.0","type":"cmd","seq":8,"src":"web","dst":0,"body":{"device":"rgb","action":"set_color","param":{"r":0,"g":255,"b":0}}}` | 5 号板执行（dst=0 是广播） |
| 遗嘱 | 把任意一块板**直接断电**（不是按重启） | 几秒后订阅区收到该板的 `sys offline`，语音播报「X 号设备已离线」 |
| 上线 | 重新上电 | 收到 `sys online`，语音播报「X 号设备已上线」 |

---

## 五、出问题时先查这几项

现象与原因按概率排序：

| 现象 | 先查什么 |
|---|---|
| 串口卡在「连接 WiFi」 | 热点名字/密码（八个工程各自 `my_config.h`），热点是否为 2.4 GHz |
| 串口一直「MQTT 连接中」 | 服务器地址、端口；教室网络是否放行 1883 |
| 蜂鸣器不响 | **引脚是不是还是占位值 26**；有源/无源与 `BUZZER_USE_PWM` 是否匹配 |
| 舵机不动或乱转 | 5V 独立供电与共地（不能从 ESP32 的 3.3V 取电） |
| 语音完全没声音 | 8E1 与 115200 两个串口参数；模块独立供电 |
| 联动没反应 | 那两块板是不是都在线（看 online 主题）；执行器板的 `SUB_EXTRA_REPORT` 是否为 1 |
| 平台命令没反应 | `dst` 是否是本板板号或 0；`body.device` 与固件里的是否一致 |
| 一点火就报、移开还报 | 6 号板基线校准期间有火源，重新上电并保持 2 秒无火 |

订阅 `202609_PP2/#` 时如果看不到某块板的报文，先确认它发出的
`src` 与板号一致——主题是精确匹配，写错了不会有任何报错。
